title Sequence Diagram #1: controller

participant Client
participant NetworkController
participant MainController

participant MoveController
participant BuildController
Client->NetworkController:News

NetworkController->MainController:forwardNews

note right of MainController:news is checked with isValidNews();\nif invalid\nGametable.setNews(INVALIDNEWS)\nthen stops
alt newstype = MOVE
MainController->MoveController:handleMove()
else BUILD


MainController->BuildController:handleBuild()
else PASS
note right of MainController:calls Gametable.nextTurn() and gets new currentPlayer\n
end